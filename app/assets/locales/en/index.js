const navigation = require("./navigation.json")
const placeholder = require("./placeholder.json")
const txt = require("./txt.json")
const btn = require("./btn.json")

export { btn, txt, navigation, placeholder }
